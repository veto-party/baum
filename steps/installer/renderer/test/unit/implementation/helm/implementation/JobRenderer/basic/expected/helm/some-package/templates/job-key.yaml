"apiVersion": "batch/v1"
"kind": "job"
"metadata": 
  "name": "some-package-key"
  "annotations": 
    "helm.sh/hook": "hello, workd"
    
  
"spec": 
  "template": 
    "restartPolicy": "OnFailure"
    {{ if eq .Values.global.registry.type "secret" }}
    "imagePullSecrets":   
      - "name": "pull-secret"
        
    {{ end }}
    "containers": 
      
      - "name": "job-some-package-key"
        "image": 
          "image": "@mockworkspace-image"
          
        "resources": 
        
        "nodeSelector": 
          "label": "somejob-some-package"
          
        "env": 
        
        
    
  
